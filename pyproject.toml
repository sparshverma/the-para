[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "interview-assistant"
version = "0.1.0"
description = "Compliant interview coaching engine prototype"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Codex"}]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
